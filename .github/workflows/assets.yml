name: Compile assets

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  assets:
    runs-on: ubuntu-16.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 12
      - run: npm install
      - run: npm run build
      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Build assets dist
