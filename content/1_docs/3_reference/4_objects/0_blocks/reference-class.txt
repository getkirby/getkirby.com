Class: Kirby\Cms\Blocks

----

Since: 3.5.0

----

Text:

## What are blocks?

When you want to work with the content of a Blocks field, you would use the `toBlocks` field method to convert it to a Blocks collection. The Blocks collection makes it easy to render the entire set of blocks or loop through blocks and create individual HTML for them.

(docs: blocks/blocks)
