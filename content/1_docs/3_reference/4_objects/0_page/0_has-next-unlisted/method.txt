Text: 

## Example

```php
echo $page->hasNextUnlisted(); //will echo 1 if true, nothing if false
```

### With collection as argument

```php
$collection = $page->siblings(false)->unlisted()->sortBy('date', 'desc');
echo $page->hasNextUnlisted($collection);
```