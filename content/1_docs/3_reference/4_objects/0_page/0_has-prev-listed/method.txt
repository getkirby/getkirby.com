Text: 

## Example

```php
echo $page->hasPrevListed(); //will echo 1 if true, nothing if false
```

### With collection as argument

```php
$collection = $page->siblings(false)->listed()->sortBy('date', 'desc');
echo $page->hasPrevListed($collection);
```