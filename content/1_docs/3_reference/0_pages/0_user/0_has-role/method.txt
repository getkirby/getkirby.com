Title:

{{class.instance}}->hasRole($role)

----

Excerpt:

Checks if the user has a particular role.

----

Return:

type: boolean
text:

----

Text:

## Example

```php
<?php if($user = $kirby->user() and $user->hasRole('editor')): ?>
This super secret content is only visible for editors
<?php endif ?>
```
