Title:

{{class.instance}}->dataUri()

----

Excerpt:

Returns a full base64 encoded data uri with a proper mime type

----

Return:

type: string
text:

----

Text:

## Example

```php
if($file = $page->file()):
    echo $file->dataUri();
endif;
```
