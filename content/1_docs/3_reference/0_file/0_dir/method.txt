Title: {{class.instance}}->dir()

----

Excerpt: Returns the full path for the directory, in which the file is located

----

Return:

type: string
text:

----

Text:

## Example:

```php
if($image = $page->image()):
    echo $file->dir();
endif;

```
