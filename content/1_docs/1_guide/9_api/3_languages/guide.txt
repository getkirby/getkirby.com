Title: Languages

----

Text:

## Language fields

The following fields are available in the language model and can be fetched with the `select` parameter.

(api-fields: language)

## Activating languages

<info>The multi-language setup needs to be activated in your config. Otherwise the following endpoints are not available.</info>

To activate multi-language mode, set the following in your config:

```php "/site/config/config.php"
return [
    'languages' => true
];
```

## Available Endpoints

(reference: api/languages)

